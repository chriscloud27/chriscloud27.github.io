import Slide from "@/components/slides/Slide";
import {
  Eyebrow,
  Headline,
  Sub,
  CompassFlow,
  InfoBox,
} from "@/components/slides/MDXComponents";

<Slide variant="dark">
  <Eyebrow>Platform Compass · How It Works</Eyebrow>
  <Headline>Survey. Score.<br/><em>Clear direction.</em></Headline>
  <Sub>A fast or deep survey feeds the scoring engine — and the engine returns an outcome tier with a concrete next step.</Sub>

<CompassFlow />

  <InfoBox>
    The Basic path delivers a lead-qualified finding in under 5 minutes. The Intense path runs WAF++ pillars over days, producing a full assessment with root-cause findings. Both paths converge on the same three outcome tiers.
  </InfoBox>
</Slide>
