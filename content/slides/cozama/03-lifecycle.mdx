import Slide from "@/components/slides/Slide";
import { Eyebrow, Headline, TwoCol } from "@/components/slides/MDXComponents";

export const stages = [
  {
    emoji: "💡",
    label: "Idea",
    desc: "Platform Compass Scorecard",
    accent: false,
  },
  {
    emoji: "🌱",
    label: "Seed",
    desc: "AI-Native Architecture · Schnelle Orientierung · Firefighting vermeiden · Kostenkontrolle",
    accent: false,
  },
  {
    emoji: "📈",
    label: "Series A / B",
    desc: "World-class Cloud Architecture · WAF2p-backed · VC-DD-ready · Big-4 Know-how zur Startup-Speed",
    accent: false,
  },
  {
    emoji: "🚀",
    label: "EXIT",
    desc: "Architecture-ready · Investor-ready · Scale-ready",
    accent: true,
  },
];

<Slide variant="dark">
  <Eyebrow>Slide 3</Eyebrow>
  <Headline>Startup-Lifecycle —<br/><em>Wo MaCh2.Cloud eingreift</em></Headline>

  <TwoCol
    left={
      <div className="flex flex-col gap-3 mt-2">
        {stages.map((s, i) => (
          <div key={i} className="flex gap-4 items-start">
            <div className={`shrink-0 w-1 self-stretch rounded-full ${s.accent ? "bg-[#00E5FF]" : "bg-white/10"}`} />
            <div className={`flex-1 border rounded-xl p-3 ${s.accent ? "border-[#00E5FF]/60 bg-[#00E5FF]/5" : "border-white/10 bg-white/5"}`}>
              <div className={`font-display text-sm font-semibold mb-0.5 ${s.accent ? "text-[#0B1F3A]" : "text-white"}`}>
                <span className="mr-2">{s.emoji}</span>
                <span className={s.accent ? "text-[#0B1F3A]" : "text-white"}>{s.label}</span>
              </div>
              <div className={`font-body text-xs leading-relaxed ${s.accent ? "text-[#0B1F3A]/80" : "text-white/50"}`}>{s.desc}</div>
            </div>
          </div>
        ))}
      </div>
    }
    right={
      <div className="flex flex-col gap-6">
        <div className="border border-white/10 rounded-xl p-5 bg-white/5">
          <p className="font-mono text-xs uppercase tracking-widest text-[#00E5FF] mb-3">Für CTOs / VP Engineering</p>
          <p className="font-body text-sm text-white/70 leading-relaxed">
            Schnelle Ergebnisse in Firefighting, Kosten, Team, Shipping.
          </p>
        </div>
        <div className="border border-[#00E5FF]/30 rounded-xl p-5 bg-[#00E5FF]/5">
          <p className="font-mono text-xs uppercase tracking-widest text-[#00E5FF] mb-3">Für Investoren</p>
          <p className="font-body text-sm text-white/70 leading-relaxed">
            Technisches Risiko sichtbar gemacht — und eliminiert.
          </p>
        </div>
      </div>
    }
  />
</Slide>
