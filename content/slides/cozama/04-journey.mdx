import Slide from "@/components/slides/Slide";
import { Eyebrow, Headline } from "@/components/slides/MDXComponents";

export const Arrow = () => (
  <div className="flex items-center justify-center shrink-0 self-center px-1">
    <svg width="18" height="10" viewBox="0 0 20 10" fill="none">
      <path
        d="M0 5H16M16 5L11 1M16 5L11 9"
        stroke="#00E5FF"
        strokeWidth="1.5"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  </div>
);

export const Node = ({ label, sub, highlight = false }) => (
  <div
    className={`rounded-lg p-2.5 text-center border ${highlight ? "border-[#00E5FF]/60 bg-[#00E5FF]/10" : "border-white/10 bg-white/5"}`}
  >
    <div
      className={`font-mono text-[11px] font-semibold leading-tight ${highlight ? "text-[#00E5FF]" : "text-white"}`}
    >
      {label}
    </div>
    {sub && (
      <div className="font-body text-[10px] text-white/40 mt-1 leading-tight">
        {sub}
      </div>
    )}
  </div>
);

export const ColHeader = ({ label }) => (
  <div className="font-mono text-[9px] uppercase tracking-[0.18em] text-[#00E5FF]/50 mb-2 text-center">
    {label}
  </div>
);

<Slide variant="dark">
  <Eyebrow>Slide 4</Eyebrow>
  <Headline>User Journey —<br/><em>omaship × MaCh2.Cloud</em></Headline>

  <div className="mt-4 w-full overflow-x-auto">
    <div className="min-w-[640px]">
      {/* DEV row */}
      <div className="mb-2">
        <div className="font-mono text-[9px] uppercase tracking-widest text-[#00E5FF]/40 mb-2">omaship-Entwicklung</div>
        <div className="flex items-stretch gap-1">
          <Node label="Enterprise Arch" sub="Know-how" />
          <Arrow />
          <Node label="Anforderungen" sub="& Foresight" />
          <Arrow />
          <Node label="WAF++" sub="7 Pillars" />
          <Arrow />
          <Node label="Qualitätsgeprüfter" sub="Standard" />
          <Arrow />
          <Node label="EU-Souveränität" sub="sichergestellt" />
        </div>
      </div>

      {/* Arrow down */}
      <div className="flex justify-end pr-4 my-1">
        <svg width="10" height="18" viewBox="0 0 10 20" fill="none">
          <path d="M5 0V16M5 16L1 11M5 16L9 11" stroke="#00E5FF" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      </div>

      {/* PLATFORM row */}
      <div className="mb-2">
        <div className="font-mono text-[9px] uppercase tracking-widest text-[#00E5FF]/40 mb-2">omaship als Plattform</div>
        <div className="flex items-stretch gap-1">
          <Node label="omaship wird" sub="Gold Standard" />
          <Arrow />
          <Node label="USP vs. Emergent" sub="durch Semantic OS" />
          <Arrow />
          <Node label="MaCh2.Cloud" sub="wirbt für omaship" />
        </div>
      </div>

      {/* Arrow down */}
      <div className="flex justify-end pr-4 my-1">
        <svg width="10" height="18" viewBox="0 0 10 20" fill="none">
          <path d="M5 0V16M5 16L1 11M5 16L9 11" stroke="#00E5FF" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      </div>

      {/* GROWTH row */}
      <div>
        <div className="font-mono text-[9px] uppercase tracking-widest text-[#00E5FF]/40 mb-2">Wachstum</div>
        <div className="flex items-stretch gap-1">
          <Node label="Jede Startup-Iteration" sub="verbessert omaship" highlight />
          <Arrow />
          <Node label="Zinseszinseffekt" sub="durch Ecosystem-Lernen" highlight />
        </div>
      </div>
    </div>

  </div>
</Slide>
