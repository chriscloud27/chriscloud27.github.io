import Slide from "@/components/slides/Slide";
import { Eyebrow, Headline } from "@/components/slides/MDXComponents";

export const Arrow = () => (
  <div className="flex items-center justify-center shrink-0 self-center px-1">
    <svg width="16" height="10" viewBox="0 0 20 10" fill="none">
      <path
        d="M0 5H16M16 5L11 1M16 5L11 9"
        stroke="#00E5FF"
        strokeWidth="1.5"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  </div>
);

export const pairs = [
  {
    startplatz: { label: "KI Accelerator", sub: "Batch-Format" },
    mach2: { label: "Platform Compass", sub: "Standard-Assessment im Batch" },
  },
  {
    startplatz: { label: "Open-Source-KI", sub: "EU-Souveränität" },
    mach2: { label: "WAF++ Sovereignty", sub: "EU-souverän by design" },
  },
  {
    startplatz: { label: "KI-Weiterbildung", sub: "Community" },
    mach2: { label: "Architecture-Literacy", sub: "für Gründer" },
  },
  {
    startplatz: { label: "Corporate Partnerships" },
    mach2: {
      label: "Technisch saubere Startups",
      sub: "als attraktivere Partner",
    },
  },
];

<Slide variant="dark">
  <Eyebrow>Slide 6</Eyebrow>
  <Headline>Startplatz —<br/><em>Gemeinsame Mission</em></Headline>

  <div className="mt-4 flex flex-col gap-3 w-full">
    <div className="grid grid-cols-[1fr_auto_1fr] gap-2 items-center">
      <div className="font-mono text-[9px] uppercase tracking-widest text-[#00E5FF]/40">Startplatz</div>
      <div className="w-16" />
      <div className="font-mono text-[9px] uppercase tracking-widest text-[#00E5FF]/40">cozama + MaCh2.Cloud</div>
    </div>
    {pairs.map((p, i) => (
      <div key={i} className="grid grid-cols-[1fr_auto_1fr] gap-2 items-center">
        <div className="border border-white/10 rounded-lg p-2.5 bg-white/5">
          <div className="font-mono text-[11px] font-semibold text-white leading-tight">{p.startplatz.label}</div>
          {p.startplatz.sub && <div className="font-body text-[10px] text-white/40 mt-0.5">{p.startplatz.sub}</div>}
        </div>
        <Arrow />
        <div className="border border-white/10 rounded-lg p-2.5 bg-white/5">
          <div className="font-mono text-[11px] font-semibold text-white leading-tight">{p.mach2.label}</div>
          {p.mach2.sub && <div className="font-body text-[10px] text-white/40 mt-0.5">{p.mach2.sub}</div>}
        </div>
      </div>
    ))}

    <div className="mt-2 border border-[#00E5FF]/60 rounded-xl p-4 bg-[#00E5FF]/5 text-center">
      <div className="font-mono text-xs font-bold text-[#00E5FF] leading-relaxed">
        KI-Mission MaCh2.Cloud: Agentic Teams auf AI‑Native Cloud & Platform Architecture · EU-souverän · WAF++-backed
      </div>
    </div>

    <div className="border-l-2 border-[#00E5FF] pl-4 mt-1">
      <p className="font-body text-xs text-white/50 leading-relaxed italic">
        cozama + MaCh2.Cloud können den Startplatz KI-Accelerator zum technisch glaubwürdigsten in der DACH-Region machen — das Programm, das Startups nicht nur funded, sondern architecture-ready in die nächste Runde schickt.
      </p>
    </div>

  </div>
</Slide>
